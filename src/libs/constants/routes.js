export const ROUTES = {
    HOME: "/",

    GROUP_DETAIL: (id) => `/group/${id}`,
    GROUP_TASKS: (id) => `/group/${id}/tasks`,
    GROUP_MATERIAL: (id) => `/group/${id}/material`,
    GROUP_STUDENTS: (id) => `/group/${id}/students`,

};
